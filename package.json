{
  "name": "server",
  "version": "1.0.0",
  "description": "- `master` : 정식 배포용\r - `develop` : 다음 버전 개발용\r     - `master`에서 분기, 작업 후 `master`로 병합\r - `feature/기능명` : 특정 기능 개발용\r     - `develop`에서 분기, 작업 후 `develop`으로 병합\r - `hotfix` : `master` 브랜치의 오류 수정용\r     - `master`에서 분기, 작업 후 `master`로 병합",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "env-cmd nodemon index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/22-2-CauCapstone-Team4/Server.git"
  },
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/22-2-CauCapstone-Team4/Server/issues"
  },
  "homepage": "https://github.com/22-2-CauCapstone-Team4/Server#readme",
  "dependencies": {
    "env-cmd": "^10.1.0",
    "express": "^4.18.2",
    "mongoose": "^6.6.6"
  },
  "devDependencies": {
    "nodemon": "^2.0.20"
  }
}
